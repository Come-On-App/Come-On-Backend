spring:
  config:
    additional-location: config/
  profiles:
    group:
      local: "local,root,datasource,jwt,oauth,aws,logging"
      dev: "dev,root,datasource,jwt,oauth,aws,logging"
      prod: "prod,root,datasource,jwt,oauth,aws,logging"
    active: local
  messages:
    basename: errors
